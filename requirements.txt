# Breeze TTS 2 audiobook front-end / renderer (Python side).
#
# The TTS itself is the C++/GGUF runtime in third_party/Breeze-TTS-2.cpp, built
# into outputs/breeze-cpp/build/ (no Python torch needed). These are only the
# Python helpers: audio IO, ASR, loudness, and the LLM client for marking.
# Python helpers: audio IO, ASR, loudness, and the LLM client for marking.
numpy==2.5.2
soundfile==0.14.0
scipy==1.18.1
pyloudnorm==0.2.0

# Local ASR (CTranslate2) for reference transcripts and QA.
faster-whisper==1.2.1

# LLM client for the script-marking stage (OpenAI-compatible local server).
openai==3.13.0

# QA / tooling scripts
pypinyin==0.55.0
requests==2.34.2
